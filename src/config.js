// URL base de tu API de Laravel (Oracle Cloud). En desarrollo local podés
// apuntar a "http://localhost:8000" (el puerto de `php artisan serve`)
// definiendo REACT_APP_API_URL en un archivo .env en la raíz de boda-app.
export const API_BASE_URL = process.env.REACT_APP_API_URL || "https://api.tudominio.com";
